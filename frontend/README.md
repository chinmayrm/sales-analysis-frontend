# Sales Trend Analyzer - Frontend (Single HTML)

- Host this `index.html` on GitHub Pages (upload to a repo, enable Pages).
- Edit the `BACKEND_URL` constant inside the `<script>` tag to point to your Render backend.
